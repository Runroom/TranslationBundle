framework:
    test: true
    secret: secret

security:
    firewalls:
        main:
            anonymous: lazy

doctrine:
    dbal:
        driver: pdo_sqlite
        path: ':memory:'
        memory: true
    orm:
        auto_mapping: true

parameters:
    locale: 'en'
    locales: ['en', 'es', 'ca']
